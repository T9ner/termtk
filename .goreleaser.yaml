# GoReleaser Configuration for Nod
# For documentation, see https://goreleaser.com
# yaml-language-server: $schema=https://goreleaser.com/static/schema.json

version: 2
project_name: nod

release:
  prerelease: auto

before:
  hooks:
    - go mod tidy

builds:
  - id: nod
    main: ./cmd/nod
    binary: nod
    goos:
      - darwin
      - windows
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}

  - id: nod-relay
    main: ./cmd/nod-relay
    binary: nod-relay
    goos:
      - darwin
      - windows
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}

archives:
  - id: nod-archive
    ids:
      - nod
      - nod-relay
    formats:
      - zip
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - docs/**/*

# Uncomment when T9ner/homebrew-tap repo is created on GitHub:
# homebrew_casks:
#   - name: nod
#     repository:
#       owner: T9ner
#       name: homebrew-tap
#     commit_author:
#       name: GoReleaser Bot
#       email: bot@goreleaser.com
#     homepage: "https://github.com/T9ner/nod"
#     description: "Offline-first, decentralized terminal messenger"
#     license: "MIT"
